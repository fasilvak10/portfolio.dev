<header id="navbar" class="fixed top-0 left-0 right-0 flex justify-center items-center py-5 w-full mx-auto z-50 transition-all duration-300">
  <div id="navbar-bg" class="absolute inset-0 opacity-0 transition-all duration-500"></div>
  
  <!-- Contenedor principal con forma de píldora -->
  <div id="navbar-pill" class="relative flex justify-between items-center w-full max-w-[800px] mx-auto px-8 py-3 rounded-full transition-all duration-300">
    
    <!-- Logo -->
    <div class="logo-container gap-3 text-2xl flex items-center z-10">
      <img
        width="36"
        height="36"
        src="https://img.icons8.com/stickers/50/finn.png"
        alt="finn"
      />
      <a href="#home" target="" class="logo text-s text-white"><i>f</i>asilva.dev</a>
    </div>

    <!-- Navegación -->
    <nav id="nav-menu" class="hidden md:flex md:flex-row md:gap-x-10 z-10">
      <a href="#projects" class="text-white/80 hover:text-white transition-colors">Projects</a>
      <a href="#experience" class="text-white/80 hover:text-white transition-colors">Experience</a>
      <a href="#contacto" class="text-white/80 hover:text-white transition-colors">Contact</a>
    </nav>
    
    <!-- Botón de menú móvil -->
    <button id="mobile-menu-toggle" class="md:hidden z-10">
      <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
      </svg>
    </button>
    
    <!-- Menú móvil -->
    <div id="mobile-menu" class="absolute top-full left-0 right-0 mt-2 hidden flex-col items-center bg-gray-900/90 backdrop-blur-md rounded-lg py-4 z-20">
      <a href="#projects" class="py-2 text-white/80 hover:text-white">Proyectos</a>
      <a href="#experience" class="py-2 text-white/80 hover:text-white">Experiencia</a>
      <a href="#contacto" class="py-2 text-white/80 hover:text-white">Contacto</a>
    </div>
  </div>
</header>

<!-- Añadir un padding-top al primer elemento después del header para compensar el espacio del navbar fixed -->
<div class="pt-[80px]"></div>

<style>
  /* Estilos adicionales para el navbar */
  #navbar-pill {
    background-color: #3e3ead8f;
    
   
  }
  
  /* Gradiente superior opcional como en tu segunda imagen */
  body::before {
    content: '';
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    /* background: linear-gradient(90deg, #4338ca, #7e22ce); */
    z-index: 100;
  }
</style>